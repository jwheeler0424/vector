export interface RouterTrieInterface<DataType> {
  // insert(value: string, data: DataType): void;
  // find(value: string): IRadixNode<DataType> | undefined;
  // remove(key: string | string[]): void;
  // contains(key: string | string[]): boolean;
  // startsWith(prefix: string): Promise<Array<string>>;
  // reset(): void;
  // display(title: string, lineConnector: string): void;
  // toObject(): Record<string, unknown>
  // toString(): string;
  // getDepth(): number;
}